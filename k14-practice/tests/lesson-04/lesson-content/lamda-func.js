/*
- Cú pháp: (parameters) => {
        //code 
    }
*/

// Hàm có nhiều tham số
let sum = (a, b) => {
    return a + b;
};
const total = sum(5, 6);
console.log(total)

// Hàm có 1 tham số 
let dup = x => {
    return x * 2;
}

// Ham chỉ có 1 biểu thức return
let dup2 = x => x * 2;
function dup2(x) {
    ///
}

// Hàm không có tham số 
() => {
    console.log("");
}

